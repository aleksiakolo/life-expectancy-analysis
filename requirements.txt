# Install the package with all extras (recommended)
# pip install -e ".[dev,advanced,sequence]"
#
# Or install individual groups:
#   pip install -e ".[dev]"           # core + dev/test tools
#   pip install -e ".[dev,advanced]"  # + XGBoost, LightGBM, CatBoost
#   pip install -e ".[dev,advanced,sequence]"  # + PyTorch (LSTM)
-e ".[dev,advanced,sequence]"
